[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
preDeployCommand = 'php artisan migrate --force && php artisan db:seed --class="Database\\Seeders\\ProductionStarterSeeder" --force'
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
